# PENDING

The certificate schema (`certify_schema_v0.1.json`) is not yet part of this snapshot; it lands here from the week-1 build. It will define the machine-readable format tying each claim in `PROVENANCE.md` §3 to its certificate file, its verifier, and its expected pass condition.

Until it lands, the claims table in `README.md` and `PROVENANCE.md` §3 is the authoritative claim-to-certificate mapping.
